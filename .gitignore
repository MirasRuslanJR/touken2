# config.js is intentionally committed right now — it ships with FORCE_DEMO: true
# and zero real secrets, so the deploy works with no Render build step at all.
# If you ever put real Supabase/Gemini credentials in it again, re-add "config.js"
# to this file BEFORE committing.
.DS_Store
*.log
